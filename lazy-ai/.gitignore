node_modules/
.env
.env.*
!.env.example
*.bak
dist/
out/
*.log

# Bundled Whisper model — large binaries, fetched by `npm run setup:models`
# (re-downloadable; not committed to keep the repo lean).
assets/models/
